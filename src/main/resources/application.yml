spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/erp
    username: root
    password: 1234
    driver-class-name: org.mariadb.jdbc.Driver
  jpa:
    hibernate:
#      ddl-auto: create-drop
      ddl-auto: update

      # create-drop, update, validate, none

    show-sql: true

    properties:
      hibernate:
        format_sql: true
mybatis:
  mapper-locations: classpath:/mapper/**/*.xml #????
  type-aliases-package: com.logitics.erp #xml??? ????, resultType ?? ???
  configuration: #?????
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

server:
  port: 33000