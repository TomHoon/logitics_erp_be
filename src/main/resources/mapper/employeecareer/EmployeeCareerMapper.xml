<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.logitics.erp.employeecareer.mapper.EmployeeCareerMapper">

    <select id="getEmployeeCareerInfo"
        resultType="EmployeeCareerInfo"
    >
        SELECT * FROM employee_career ec
        WHERE ec.employee_id = #{employeeId}
    </select>

    <insert
        id="addInfo"
        parameterType="EmployeeCareerAddInfoRequest"
    >
        INSERT INTO employee_career(
            employee_id,
            company_name,
            hire_date,
            resignation_date,
            position_name,
            department_name,
            resignation_reason,
        )
        VALUES (
            #{r.employeeId},
            #{r.companyName},
            #{r.hireDate},
            #{r.resignationDate},
            #{r.positionName},
            #{r.departmentName},
            #{r.resignationReason}
        )
    </insert>


    <update id="deleteInfo">
        UPDATE employee_career ec
            SET ec.deleted = true
            WHERE ec.employee_career_id = #{deletedCareerId}
    </update>


</mapper>