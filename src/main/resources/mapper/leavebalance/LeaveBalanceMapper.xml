<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.logitics.erp.leavebalance.mapper.LeaveBalanceMapper">

    <select id="getBasicInfo">
        SELECT * FROM leave_balance lb
        left join attendance a
            on a.employee_id = lb.employee_id
            and a.work_date = CURDATE()
        where lb.employee_id = #{employeeId}
    </select>

</mapper>